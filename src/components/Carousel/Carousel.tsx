import {useState} fromn 'react';
import { View, Text, StyleSheet } from 'react-native';
import colors from '../../assets/themes/colors';

const Carousel = () => {
  return (
    <View>
      <Text>Carousel</Text>
    </View>
  );
}

export default Carousel;


const styles = StyleSheet.create({
    
});